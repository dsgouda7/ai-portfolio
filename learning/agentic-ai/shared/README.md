# Shared OrderFlow Support

This package contains only track-wide deterministic fixtures, provider/service doubles, token and trace accounting, and common assertions.

Agent loops, planners, memory strategies, graph construction, policy enforcement, protocols, and recovery mechanisms stay inline in their teaching notebooks so framework abstractions do not hide the mechanism being learned.

The shared package performs no network calls, reads no credentials, and creates no persistent side effects by default.
