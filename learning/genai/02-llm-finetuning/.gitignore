# Chapter-local environment created by setup.ps1 / setup.sh.
.venv/

# Fine-tuned checkpoints produced while running the notebook.
checkpoints/
*.safetensors
*.bin

# Preserve the committed teaching checkpoints moved with this chapter.
!checkpoints/
!checkpoints/**
