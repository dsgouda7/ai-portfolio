# ── Core ──────────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# ── LLM client (covers both Ollama and Azure AI Foundry) ──────────────────────
openai>=1.50.0

# ── Local STT: faster-whisper ─────────────────────────────────────────────────
faster-whisper>=1.0.0

# ── Local TTS: edge-tts (Microsoft Edge neural voices, no API key needed) ─────
edge-tts>=6.1.0

# ── Azure Speech SDK (STT + TTS) — only needed when *_PROVIDER=azure ──────────
# Uncomment if you intend to use Azure Speech:
# azure-cognitiveservices-speech>=1.38.0
