# vector-store.env.example
# Vector store service — ChromaDB + semantic LRU cache.
# No external credentials required; all config is structural.

EMBEDDING_SERVICE_URL=http://embedding:8002
CHROMA_PERSIST_DIR=/data/chroma_db
CACHE_SIZE=1000
CACHE_THRESHOLD=0.85
